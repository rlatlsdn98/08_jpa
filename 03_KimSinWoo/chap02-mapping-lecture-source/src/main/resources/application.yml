spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/menudb
    username: swcamp
    password: swcamp
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create # @Entity ??? ??? @Table("????")? DB? ??? ????,
    properties:
      hibernate:
        format_sql: true